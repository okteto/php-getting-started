build:
  hello-world:
    image: okteto.dev/php-hello-world:1.0.0
    context: .
deploy:
  - kubectl apply -f k8s.yml
dev:
  hello-world:
    command: bash
    sync:
      - .:/app
    reverse:
      - 9000:9000
    volumes:
      - /root/.composer/cache
